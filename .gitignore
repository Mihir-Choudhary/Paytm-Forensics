# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs
.venv/
venv/
env/

# PyInstaller build artifacts
build/
dist/
*.spec.bak

# Generated case output
case_*/
*.case/
# scratch databases from ad-hoc probes
ent_order_*.db
/*.db

# Generated reports / screenshots / one-off artifacts
report.html
report.html.sha256
shots/

# IDE
.idea/
.vscode/
*.swp

# OS
Thumbs.db
.DS_Store

# Evidence — NEVER commit
net.one97.paytm/
extraction/
*.tar
*.tar.gz
*.zip
*.ab
*.img

# Local env
.env
.env.local

# Real case identifiers for the pre-push PII gate (tools/check_no_pii.py).
# Holds real PII - must NEVER be committed.
tools/pii_patterns.local

# Per-extraction ground-truth values used by some regression tests.
# Holds real PII from the examiner's evidence — must NEVER be committed.
tests/_truth.json
